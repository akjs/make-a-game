const Phaser = require('phaser-ce');

const preload = (game) => {

};

const create = (game) => {

};

const update = (game) => {

};

new Phaser.Game(800, 600, Phaser.AUTO, 'app', { preload, create, update });

